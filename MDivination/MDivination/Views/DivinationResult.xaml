<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:viewmodels="clr-namespace:MDivination.ViewModels"
             x:DataType="viewmodels:MemViewModel"
             x:Class="MDivination.Views.DivinationResult">


        <StackLayout>
        <ImageButton Source="{Binding ImageSource}" Aspect="AspectFit" HeightRequest="250" WidthRequest="250" Command="{Binding HistoryCommand}"/>
            
     
        <Label Text="{Binding Interpretation}"  VerticalOptions="EndAndExpand" HorizontalOptions="Center" FontSize="Large" TextColor="White" BackgroundColor="Black"/>
    </StackLayout>
        
    
          
              

</ContentPage>